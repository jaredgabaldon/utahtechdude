# Utah Tech Dude

Marketing site for `utahtechdude.com`, built with Astro.

## Run locally

1. Install dependencies with `npm install`
2. Start the dev server with `npm run dev`

## Build

Run `npm run build`
